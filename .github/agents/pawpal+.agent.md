---
name: pawpal+
description: generate the "skeleton" of your classes for pawpal+ app. 
argument-hint: The inputs this agent expects includ a UML, and a description of the app. The output should be the code for the classes, with comments describing the purpose of each class and method.
# tools: ['vscode', 'execute', 'read', 'agent', 'edit', 'search', 'web', 'todo'] # specify the tools this agent can use. If not set, all enabled tools are allowed.
---

<!-- Tip: Use /create-agent in chat to generate content with agent assistance -->

Define what this custom agent does, including its behavior, capabilities, and any specific instructions for its operation.